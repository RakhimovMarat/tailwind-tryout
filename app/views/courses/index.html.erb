<%= render ListComponent.new do |c| %>
  <% c.with_header(classes: "") do %>
    <%= link_to "List of Available Courses", courses_index_path %>
  <% end %>
  <% course_titles = Course.all.pluck(:title).map { |title| {title: title}} %>
  <% c.with_titles(course_titles) %>
<% end %>
